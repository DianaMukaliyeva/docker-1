docker run  -d --name roach-warden -p 8081:80 --link spawning-pool:db phpmyadmin/phpmyadmin

# The option --link provides access to another container running in the host.
# In our case the container is the one created in the previous section, called spawning-pool and the resource accessed is the MySQL db.